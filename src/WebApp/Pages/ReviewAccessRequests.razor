@inherits Fluxor.Blazor.Web.Components.FluxorComponent

@page "/review-access-requests"
@page "/approuver-les-demandes"

@using OfficeEntry.WebApp.Shared

<h1>@Localizer["Review Access Requests"]</h1>

@if (ApprovalsState.Value.IsLoading)
{
    <Loading />
}
else
{
    <AccessRequestDatatable AccessRequests="@ApprovalsState.Value.AccessRequests"></AccessRequestDatatable>
}