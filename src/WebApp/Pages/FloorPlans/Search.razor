@inherits Fluxor.Blazor.Web.Components.FluxorComponent

@page "/en/floor-plans/search"
@page "/fr/plans-d-etages/recherche"

<h3>Search</h3>

@if (!State.Value.FloorPlans.Any())
{
    if (State.Value.IsLoading)
    {
        <Loading />
    }
    else
    {
        <p>No floor plans found.</p>
    }
}
else
{
    <button type="button" class="btn btn-secondary" @onclick=Refresh>Refresh</button>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">&nbsp;</th>
                <th scope="col">@Localizer["Name"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var floorPlan in State.Value.FloorPlans)
            {
                <tr>
                    <th scope="row" class="col-1"><a href='@Localizer["floor-plans-edit", @floorPlan.Id]'>Edit</a></th>
                    <td>@floorPlan.Name</td>
                </tr>
            }
        </tbody>
    </table>
}